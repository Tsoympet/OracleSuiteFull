OracleSuite – Unified Multi-Domain Prediction & Risk-Aware Analytics
===================================================================

This is the **skeleton + core code** bundle:

- Full backend skeleton (FastAPI) with:
  - core config/db/auth
  - lottery engine with favorites & stub prediction
  - stub routers for football, markets (crypto/stocks), casino, alerts, notifications, suite, chat
- Frontend-web skeleton (React + TS) with a minimal dashboard shell.
- Mobile-app skeleton (Expo) for future GFPS / OracleSuite client.
- Config/infra/docs/scripts placeholders.

Everything not yet implemented is a clean placeholder with TODO markers.
